# Pending changelog entry — Hero Boundary Mark R1

To be folded into `CHANGELOG.md` when this visual increment is approved and merged.

- Reworked the homepage thesis into a deliberate five-line typographic composition.
- Connected the `j` in “just” to the `l` in “line” so the typography itself crosses the boundary between lines.
- Preserved the phrase as semantic heading text rather than converting the core thesis into a raster image.
